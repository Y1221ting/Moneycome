# 纷析云财务系统 — 部署进度总结

## 一、服务器概况

| 项目 | 详情 |
|------|------|
| 服务器 | 阿里云 ECS 轻量型 2核2G |
| 系统 | Ubuntu 22.04 |
| 公网 IP | 47.96.17.173 |
| 安全组开放端口 | 22、80、443、5000 |

---

## 二、已部署的 Docker 容器

4 个容器通过自定义桥接网络 `financial-ce`（172.25.0.0/24）互联：

| 容器名 | 镜像 | IP | 端口映射 | 状态 |
|--------|------|-----|---------|------|
| `financial_mysql8` | mysql:8.2.0 | 172.25.0.2 | 3307→3306 | 运行中 |
| `financial_redis5` | redis:5.0.12 | 172.25.0.3 | 6479→6379 | 运行中 |
| `financial_client` | openjdk:8u102 | 172.25.0.5 | 8888→8012 | 运行中 |
| `financial_nginx` | nginx:1.25.2 | 172.25.0.6 | 80→80 | 运行中 |

- 网站访问地址：http://47.96.17.173（HTTP 200，正常运行）
- 后端直连：http://47.96.17.173:8888
- MySQL：47.96.17.173:3307

---

## 三、服务器文件结构

```
/opt/fenxiyun/
├── docker/                          # Docker 部署目录
│   ├── docker-compose.yml
│   ├── application.yml              # Spring Boot 配置
│   ├── .env                         # 环境变量
│   ├── mysql/data/                  # MySQL 数据（持久化）
│   ├── server/financial.jar         # Spring Boot JAR（68MB）
│   ├── nginx/
│   │   ├── vhost/                   # Nginx 配置
│   │   └── wwwroot/                 # 前端静态文件
│   └── logs/                        # 应用日志
├── bs-server/                       # 纷析云后端源码
├── front-end/                       # 纷析云前端源码
├── kernel/                          # 核心模块
└── financial.sql                    # 数据库初始化脚本
```

---

## 四、数据库

### 连接信息

| 项目 | 值 |
|------|-----|
| 数据库名 | financial |
| 用户 | root |
| 密码 | 见 密码备忘.secret（不入库） |
| 表前缀 | fxy_financial_ |
| 表数量 | 24 张 |

### 核心业务表（fxy_financial_ 前缀）

| 表名 | 功能 |
|------|------|
| account_subject | 会计科目（科目编码） |
| voucher | 记账凭证 |
| voucher_detail | 凭证明细 |
| period | 会计期间 |
| balance_sheet | 资产负债表 |
| profit_statement | 利润表 |
| cash_flow_statement | 现金流量表 |
| customer | 客户/供应商 |
| auxiliary_accounting | 辅助核算 |
| period_settle | 期末结转 |
| fixed_assets | 固定资产 |
| bank_account | 银行账户 |
| tax_rate | 税率 |
| currency | 币种 |
| department | 部门 |
| project | 项目 |
| employee | 员工 |
| inventory | 存货 |
| log | 操作日志 |
| system_config | 系统配置 |
| user | 用户 |
| role | 角色 |
| permission | 权限 |
| company | 公司信息 |

---

## 五、关键配置说明

### application.yml（Spring Boot 配置）

- 数据库连接：`jdbc:mysql://172.25.0.2:3306/financial`
- 数据库密码：通过环境变量 `DB_PASSWORD` 注入
- Redis 连接：`172.25.0.3:6379`，密码通过 `REDIS_PASSWORD` 环境变量注入
- JVM 参数：`-Xms256m -Xmx512m`（适配 2G 内存，避免 OOM）
- Hikari 连接池：最大 5，最小 2
- 认证框架：sa-token，token 名为 `fsatoken`
- SQL 日志级别：`warn`（生产不打印 SQL）
- 阿里云短信配置：accessKeyId 和 accessKeySecret 均为空字符串占位

### Nginx 配置

- 前端静态文件使用 SPA 模式（`try_files $uri $uri/ /index.html`）
- `/api` 路径：去掉 `/api` 前缀后代理到后端 `financial_client:8012`
- `/server` 路径：直接代理到后端 `financial_client:8012`
- 已配置 gzip 压缩
- 安全头：X-Frame-Options、X-Content-Type-Options、X-XSS-Protection
- 代理超时：连接 60s，读取 300s，发送 60s

---

## 六、Docker Compose 配置要点

4 个服务通过静态 IP 互联，MySQL 和 Redis 配置了 healthcheck，client 使用 `condition: service_healthy` 等待依赖就绪后启动。

---

## 七、部署过程中解决的关键问题

| # | 问题 | 现象 | 解决方案 |
|---|------|------|----------|
| 1 | Docker Hub 不可达 | registry-1.docker.io 超时 | 配置国内镜像加速器 `/etc/docker/daemon.json`（1panel.live、hub.rat.dev、dao.cloud） |
| 2 | MySQL 连接拒绝 | Access denied for root | .env 文件路径不对 + 旧数据卷缓存错误密码，移动 .env 到 compose 同目录并删除旧数据 |
| 3 | 容器间 DNS 不解析 | Communications link failure | 容器间 hostname 跨 DNS 不解析，改用静态 IP 172.25.0.2 |
| 4 | Spring Boot 配置加载失败 | Could not resolve placeholder | `--spring.config.location` 必须在 `-jar xxx.jar` 之后，不能放在前面 |
| 5 | JVM 不识别参数 | unrecognized option | Spring Boot 参数是程序参数不是 JVM 参数，放在 JAR 后面即可 |
| 6 | 缺少 aliyun 配置 | Could not resolve placeholder 'aliyun.accessKeyId' | 在 application.yml 补充空的 aliyun 占位配置节点 |
| 7 | Nginx 返回 403 | 前端资源无法访问 | 容器内有旧的 conf 文件（clien.conf、http.conf）覆盖了配置，删除后重载 nginx |
| 8 | npm build 内存溢出 | 服务器 2G 内存耗尽 | 改为本地构建前端 dist，再上传到服务器 |
| 9 | 镜像拉取 TLS 错误 | GnuTLS handshake error | 配置 Docker 镜像加速器后解决 |

---

## 八、待完成工作

| # | 任务 | 优先级 | 说明 |
|---|------|--------|------|
| 1 | 创建管理员用户 | 🔴 紧急 | 数据库 `fxy_financial_user` 表目前为空，需要插入初始管理员账号才能登录 |
| 2 | 修改登录方式 | 🔴 紧急 | 系统默认登录流程涉及手机验证码（sa-token + 阿里云 SMS），需要改为用户名+密码登录 |
| 3 | 配置 HTTPS | 🟡 | 使用 Let's Encrypt / certbot 为域名配置 SSL 证书 |
| 4 | 修改默认密码 | 🟡 | 修改 MySQL、Redis 的默认密码 |
| 5 | 安全组收紧 | 🟢 | 关闭 3307、6479、8888 端口的公网访问，仅保留 80/443 |

---

## 九、GitHub 仓库

- 仓库地址：https://github.com/Y1221ting/Moneycome
- 内容：docker-config 部署配置（docker-compose.yml、application.yml、nginx conf、.env）+ README 文档 + .gitignore
- 最新提交：`9e79008` — 修复部署配置 11 个问题

---

## 十、技术栈

- 后端：Spring Boot 2.1.8 + MyBatis-Plus + MySQL 8.2.0 + Redis 5.0.12
- 前端：Vue 2 + HeyUI 组件库
- 认证：sa-token（token 名：fsatoken）
- 构建：Gradle 多模块（kernel / bs-server / front-end）
- 部署：Docker Compose

---

## 十一、常用运维命令

```bash
cd /opt/fenxiyun/docker

# 查看所有容器状态
docker compose ps

# 查看后端日志
docker compose logs -f --tail=100 client

# 重启后端
docker compose restart client

# 全部重启
docker compose down && docker compose up -d

# 完全重建（清空数据）
docker compose down -v
rm -rf mysql/data redis/
docker compose up -d

# 备份数据库
docker exec financial_mysql8 mysqldump -uroot -p"$(grep MYSQL_ROOT_PASSWORD .env | cut -d= -f2)" financial > backup_$(date +%Y%m%d).sql

# 备份全部配置
tar -czf fenxiyun_backup_$(date +%Y%m%d).tar.gz /opt/fenxiyun/docker/
```
